/*commes[0]:1
commes[1]:2
commes[2]:5

공약수를 찾는 첫 for문에는 min이 들어있지 않다. 

cnt는 commes가 나올 때마다 1씩 증가한다. cnt==49가 되면 FIN으로 간다. 

공약수 최대 개수를 100개로 해두었다. 

내건 무한.*/
/* 교수님 소스코드 짱짱. 이거 다시 리뷰해야겠다.*/
/*양질의 남의 코드를 봐야한다.*/

#include <stdio.h>
#include <time.h>

int main(void)
{
	int h = 2;
	int i;
	double start1, end1, start2, end2;

	start1 = (double)clock() / CLOCKS_PER_SEC;
	for (i = 0; i <= h; i++)
	{
		printf("안녕하세요.\n");
	}
	end1 = ((double)clock()) / CLOCKS_PER_SEC;

	start2 = (double)clock() / CLOCKS_PER_SEC;
	for (i = 0; i < h; i++)
	{
		printf("안녕하세요.\n");
	}
	end2 = ((double)clock()) / CLOCKS_PER_SEC;

	printf("프로그램 수행 시간1 :%lf\n", (end1 - start1));
	printf("프로그램 수행 시간2 :%lf\n", (end2 - start2));

}
